{
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/service/app/main.py"
        }
    ],
    "builds": [
        {
            "src": "service/app/main.py",
            "use": "@vercel/python"
        },
        {
            "src": "package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "dist"
            }
        }
    ]
}